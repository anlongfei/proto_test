INCLUDE_DIRECTORIES(
    ${CMAKE_CURRENT_LIST_DIR}
)

AUX_SOURCE_DIRECTORY("${CMAKE_CURRENT_LIST_DIR}" SRC)

SET(SRC_LIST ${SRC})

ADD_LIBRARY(so1 SHARED ${SRC_LIST})

target_link_libraries(so1
    pb1
)
install(TARGETS so1 DESTINATION lib64)
